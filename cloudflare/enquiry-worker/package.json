{
  "name": "yunnan-unfolded-enquiry-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "types": "wrangler types --include-runtime false",
    "check": "tsc --noEmit",
    "test": "node --experimental-strip-types test/contract.test.mjs",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "secret:resend": "wrangler secret put RESEND_API_KEY"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "wrangler": "^4.0.0"
  }
}
